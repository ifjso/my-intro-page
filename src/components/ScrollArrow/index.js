/* --------------------------------------------------------
author: Dval
----------------------------------------------------------*/

import React from 'react';
import './ScrollArrow.scss';

const ScrollArrow = () => <div className={`scroll-arrow img-scroll-arrow blink-smooth`} />;

export default ScrollArrow;
